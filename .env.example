# Example environment file. Copy to `.env` and adjust as needed.
# The demo runs entirely on defaults, so these are optional.

# Backend
# PORT=8080

# Frontend (Vite) - base URL the dev server proxies /api to
# VITE_API_TARGET=http://localhost:8080
